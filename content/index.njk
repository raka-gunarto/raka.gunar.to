---js
const eleventyNavigation = {
	key: "Home",
	order: 1
};

const numberOfLatestPostsToShow = 3;
---
<h1>Hi!</h1>
<p>Welcome to <a href="/about/">Raka Gunarto</a>'s personal blog. Here I write mostly about technology and software development, and occasionally about other topics that interest me.</p>
{% set postsCount = collections.activePosts | length %}
{% set latestPostsCount = postsCount | min(numberOfLatestPostsToShow) %}
<h1>Latest Post{% if latestPostsCount != 1 %}s{% endif %}</h1>

{% set postslist = collections.activePosts | head(-1 * numberOfLatestPostsToShow) %}
{% set postslistCounter = postsCount %}
{% include "postslist.njk" %}

{% set morePosts = (collections.posts | length) - (postslist | length) %}
{% if morePosts > 0 %}
<p>{{ morePosts }} more post{% if morePosts != 1 %}s{% endif %} can be found in <a href="blog.njk">the archive</a>.</p>
{% endif %}

{# List every content page in the project #}
{#
<ul>
	{%- for entry in collections.all %}
	<li><a href="{{ entry.url }}"><code>{{ entry.url }}</code></a></li>
	{%- endfor %}
</ul>
#}
